# plugins

Contains plugins and adapters that extend the assistant with connectors to external systems.

Suggested subfolders:
- adapters/   — generic adapters for vector DBs, file stores, 3rd-party APIs
- sample/     — example plugin implementations
- spec/       — plugin API spec and guidelines for authors
