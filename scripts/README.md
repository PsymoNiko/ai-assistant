# scripts

Useful developer scripts for local setup, migrations, and maintenance.

Examples:
- scripts/bootstrap.sh     — sets up local env
- scripts/migrate.sh       — runs DB migrations
- scripts/generate-sdk.sh  — builds SDK packages
