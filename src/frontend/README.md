# src/frontend

Frontend demo and UI for the ai-assistant project.

Suggested subfolders:
- web/         — React or Svelte app for chat UI
- components/  — reusable UI components (ChatWindow, Message, Composer)
- styles/      — CSS / Tailwind / design tokens
- static/      — images and assets

Include a minimal demo under examples/ for quick local startup.
