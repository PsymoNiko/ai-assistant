# Gateway Service (FastAPI)

This service implements the FastAPI gateway described in the architecture diagram. It exposes HTTP and WebSocket endpoints that client apps (web, mobile, desktop, voice) use to interact with the AI platform. The gateway forwards requests to planner/agents and mediates authentication and session handling.

See docker-compose.yml at repository root for a local development environment.
