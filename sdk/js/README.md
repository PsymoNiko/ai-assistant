# sdk/js

JavaScript / TypeScript SDK for embedding the ai-assistant into applications.

Suggested contents:
- src/         — SDK source code
- examples/    — quickstart examples
- tests/       — SDK unit tests

Publishable as an npm package when stable.
